# T05 Magnetometer Model — Test Status

Generated by `test_mag_model.m` on 2026-05-21 01:21:18.

Build dir: `E:/C.A.S.P.E.R/C.A.S.P.E.R Flight Software V2/Casper 2 flight firmware/C.A.S.P.E.R.-2/Matlab Code/Simulink Development/build/T05_mag_sensor_model`

**Overall: PASS** (11 pass / 0 fail of 11)

## Acceptance Criteria

| # | Criterion | Status | Detail |
|---|---|---|---|
| C1 | pad attitude, no noise: \|mag_body\| == \|B_NED\| (rotation preserves magnitude) | PASS | \|mag\|=40.180000, \|B\|=40.180000, \|delta\|=0.00e+00 |
| C2 | round-trip identity AND calibrated \|mag\| in 40.18 ± 0.5 µT | PASS | round-trip max-abs-err=1.05e-15 µT (limit 0.1), cal_mag=40.1800 µT (target 40.18 ± 0.5) |
| C3 | AR(1) lag-1 autocorrelation ≈ exp(-dt/tau) | PASS | rho1=0.9399 vs target 0.9394 (\|delta\|<0.020) |
| C4 | PSD: colored shape (drop ≥ 6 dB from ~0.5 Hz to ~5 Hz) | PASS | drop = 13.46 dB (limit ≥ 6 dB) |
| C5 | quantization: raw_18bit ∈ [0, 262143] and mag_uT on 1-LSB grid | PASS | grid LSB = 0.006104 µT; max(\|m_uT - decode(raw18)\|) = 0.00e+00 µT; all raw in range |
| C6 | TX schedule: 15 ms active per 100 ms (1.5 sample-periods worth, 2 discrete samples) | PASS | samples_per_cycle=2.0000 (target 2, per spec §6.3); tx_duration=0.015000 s (target 0.015 s, == 1.5 sample-periods) |
| C7 | TX-on deviation ≈ ±10 µT per axis (±1 µT tolerance) | PASS | per-axis \|spike\|=[9.881 10.052 9.869] vs target 10.0 µT |
| C8 | off-TX samples within ±3 µT of clean raw signal (noise band) | PASS | max \|dev\|=1.8213 µT (limit 3.0) |
| C9 | reproducibility: identical seed -> byte-identical mag stream | PASS | bit-identical: 1 |
| C10 | build_mag_block produces a valid mag_block.slx | PASS | mag_block.slx at E:\C.A.S.P.E.R\C.A.S.P.E.R Flight Software V2\Casper 2 flight firmware\C.A.S.P.E.R.-2\Matlab Code\Simulink Development\build\T05_mag_sensor_model\mag_block.slx |
| C11 | plots: mag_pad_1s_no_tx, mag_pad_1s_with_tx, mag_tx_event_zoom exist | PASS | no_tx=1, with_tx=1, zoom=1 |

## Files Produced

- `casper_mag_field_world.m`
- `casper_mag_rotate_to_body.m`
- `casper_mag_distort_field.m`
- `casper_mag_noise.m`
- `casper_mag_radio_interference.m`
- `casper_radio_tx_schedule.m`
- `build_mag_block.m`
- `test_mag_model.m`
- `mag_block.slx`
- `plots/mag_pad_1s_no_tx.png`
- `plots/mag_pad_1s_with_tx.png`
- `plots/mag_tx_event_zoom.png`
- `plots/mag_noise_psd.png` (informational)
- `STATUS.md`

## Radio Interference — Placeholder Status

**This effect is NOT calibrated.** The ±10.0 µT rectangular pulse model is a Phase 0 placeholder. It exists to validate that the EKF + attitude filter behave robustly when mag samples are intermittently corrupted, NOT to reproduce real-world coupling magnitudes. Phase 1 will fit the amplitude and per-axis coupling matrix from bench data (see PHASE0_SPEC.md §6, T05 §9). Downstream consumers of this model must NOT treat the spike amplitude as a calibrated number.

Per-axis sign vector (seeded once from `Sim.Seed + 7 = 20260526`): [-1, +1, -1].

## Deviations / Notes

- Spec §5.4 cites quantization scale `81.92 counts/µT` (= 1 LSB ≈ 0.0122 µT). The firmware (`mmc5983ma.c` lines 184-186) decodes as `mag_uT = (raw - 131072) / 16384 * 100`, i.e. **163.84 counts/µT** (1 LSB ≈ 0.006104 µT). The implementation uses the firmware-correct values from `casper_sensor_params.m` (`Mag.ScaleCountsPerGauss = 16384`, `Mag.OffsetCounts = 131072`). The acceptance criterion was reframed in terms of "raw_18bit is integer in [0, 262143] and mag_uT is on a 1-LSB grid", which is what the spec author was actually trying to test.
- Spec mentions a calibration scaling division-by-2 inside that 81.92 derivation that is not present in the firmware; the firmware decode and the spec parenthetical "raw_18bit integer in [0, 2^18-1]" are mutually consistent, and that is what test C5 verifies.
- Plot `mag_noise_psd.png` (PSD) is generated in addition to the three required plots, as supporting evidence for C4.
- The Simulink library `mag_block.slx` is built via `build_mag_block.m` and saved as a locked library. It exposes inputs `quat_std (4x1)`, `pos_NED_m (3x1)`, `time_s (1)` and outputs `mag_uT_body (3x1)`, `mag_raw_18bit (3x1 uint32)`, `data_ready (bool)`, `tx_active (bool)`. The block runs at `1/Mag.Rate_Hz = 100 Hz` after the input Rate Transitions; the inputs themselves can be driven at the 10 kHz solver rate (typical use). All numerical parameters are bound at runtime via `evalin('base', ...)` to the structs created by `casper_sensor_params.m`.
- The Simulink library is built and persisted but not exercised in `sim()` here — the unit tests target the .m functions directly because they hold the math. Full Simulink-level integration is the responsibility of T11.
